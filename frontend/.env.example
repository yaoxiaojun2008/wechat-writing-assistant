# API Configuration
# For local development: point to your backend server
# VITE_API_URL=http://localhost:3001/api

# For production deployment on Vercel with separate backend:
# Replace with your actual backend server URL
# VITE_API_URL=https://your-backend-domain.com/api

# For Vercel deployment with Serverless Functions (all-in-one, as in current setup):
# Use relative path to point to Vercel Functions in the same project
VITE_API_URL=/api